--- a/crates/obscura-browser/src/page.rs
+++ b/crates/obscura-browser/src/page.rs
@@ -119,6 +119,14 @@
                 js.set_dom(d);
             }
 
+            if self.intercept_enabled {
+                if let Some(tx) = &self.intercept_tx {
+                    js.set_intercept_tx(tx.clone());
+                }
+            } else {
+                js.clear_intercept_tx();
+            }
+
             let _ = js.execute_script("<reset>",
                 "_cache.clear(); globalThis.__obscura_objects = {}; globalThis.__obscura_oid = 0; \
                  _iframeRegistry.length = 0; globalThis.length = 0; \
@@ -147,8 +155,10 @@
         rt.set_cookie_jar(self.context.cookie_jar.clone());
         rt.set_http_client(self.http_client.clone());
 
-        if let Some(tx) = &self.intercept_tx {
-            rt.set_intercept_tx(tx.clone());
+        if self.intercept_enabled {
+            if let Some(tx) = &self.intercept_tx {
+                rt.set_intercept_tx(tx.clone());
+            }
         }
 
         if let Some(dom) = self.dom.take() {
@@ -394,21 +404,41 @@
         url_str: &str,
         wait_until: crate::lifecycle::WaitUntil,
     ) -> Result<(), PageError> {
-        self.navigate_with_wait_post(url_str, wait_until, "GET", "").await
+        self.navigate_with_wait_post_with_preloads(url_str, wait_until, "GET", "", &[]).await
     }
 
     pub async fn navigate_with_wait_post(
+        &mut self,
+        url_str: &str,
+        wait_until: crate::lifecycle::WaitUntil,
+        method: &str,
+        body: &str,
+    ) -> Result<(), PageError> {
+        self.navigate_with_wait_post_with_preloads(url_str, wait_until, method, body, &[]).await
+    }
+
+    pub async fn navigate_with_wait_with_preloads(
         &mut self,
         url_str: &str,
         wait_until: crate::lifecycle::WaitUntil,
+        preload_scripts: &[String],
+    ) -> Result<(), PageError> {
+        self.navigate_with_wait_post_with_preloads(url_str, wait_until, "GET", "", preload_scripts).await
+    }
+
+    pub async fn navigate_with_wait_post_with_preloads(
+        &mut self,
+        url_str: &str,
+        wait_until: crate::lifecycle::WaitUntil,
         method: &str,
         body: &str,
+        preload_scripts: &[String],
     ) -> Result<(), PageError> {
         let mut current_url = url_str.to_string();
         let mut current_method = method.to_string();
         let mut current_body = body.to_string();
         for _chain in 0..10 {
-            self.navigate_single(&current_url, wait_until, &current_method, &current_body).await?;
+            self.navigate_single(&current_url, wait_until, &current_method, &current_body, preload_scripts).await?;
             if let Some((next_url, next_method, next_body)) = self.take_pending_navigation() {
                 tracing::info!("JS-triggered navigation chain: {} {} -> {}", current_method, current_url, next_url);
                 current_url = next_url;
@@ -427,6 +457,7 @@
         wait_until: crate::lifecycle::WaitUntil,
         method: &str,
         body: &str,
+        preload_scripts: &[String],
     ) -> Result<(), PageError> {
         let url = Url::parse(url_str).map_err(|e| PageError::InvalidUrl(e.to_string()))?;
 
@@ -555,10 +586,12 @@
 
         if wait_until == crate::lifecycle::WaitUntil::DomContentLoaded {
             self.init_js();
+            self.execute_preload_scripts(preload_scripts);
             return Ok(());
         }
 
         self.init_js();
+        self.execute_preload_scripts(preload_scripts);
 
         if !css_sources.is_empty() {
             if let Some(js) = &mut self.js {
@@ -638,12 +671,24 @@
     }
 
     pub async fn fulfill_navigation_html(
+        &mut self,
+        url_str: &str,
+        body_text: &str,
+        status: u16,
+        headers: &std::collections::HashMap<String, String>,
+        wait_until: crate::lifecycle::WaitUntil,
+    ) -> Result<(), PageError> {
+        self.fulfill_navigation_html_with_preloads(url_str, body_text, status, headers, wait_until, &[]).await
+    }
+
+    pub async fn fulfill_navigation_html_with_preloads(
         &mut self,
         url_str: &str,
         body_text: &str,
         status: u16,
         headers: &std::collections::HashMap<String, String>,
         wait_until: crate::lifecycle::WaitUntil,
+        preload_scripts: &[String],
     ) -> Result<(), PageError> {
         let url = Url::parse(url_str).map_err(|e| PageError::InvalidUrl(e.to_string()))?;
 
@@ -670,6 +715,7 @@
         self.lifecycle = LifecycleState::DomContentLoaded;
 
         self.init_js();
+        self.execute_preload_scripts(preload_scripts);
         if wait_until == crate::lifecycle::WaitUntil::DomContentLoaded {
             return Ok(());
         }
@@ -860,6 +906,14 @@
         }
     }
 
+    fn execute_preload_scripts(&mut self, preload_scripts: &[String]) {
+        for source in preload_scripts {
+            if let Err(e) = self.execute_preload_script(source) {
+                tracing::debug!("Preload script error: {}", e);
+            }
+        }
+    }
+
     pub fn suspend_js(&mut self) {
         if let Some(js) = &self.js {
             if let Some(dom) = js.take_dom() {
@@ -902,11 +956,21 @@
     }
 
     pub fn set_intercept_tx(&mut self, tx: tokio::sync::mpsc::UnboundedSender<obscura_js::ops::InterceptedRequest>) {
+        self.intercept_enabled = true;
         self.intercept_tx = Some(tx.clone());
         if let Some(js) = &self.js {
             js.set_intercept_tx(tx);
         }
     }
+
+    pub fn clear_intercept_tx(&mut self) {
+        self.intercept_enabled = false;
+        self.intercept_block_patterns.clear();
+        self.intercept_tx = None;
+        if let Some(js) = &self.js {
+            js.clear_intercept_tx();
+        }
+    }
 }
 
 #[derive(Debug, thiserror::Error)]
--- a/crates/obscura-cdp/src/domains/page.rs
+++ b/crates/obscura-cdp/src/domains/page.rs
@@ -46,15 +46,9 @@
                 let nav_method = params.get("__method").and_then(|v| v.as_str()).unwrap_or("GET");
                 let nav_body = params.get("__body").and_then(|v| v.as_str()).unwrap_or("");
                 if nav_method == "POST" && !nav_body.is_empty() {
-                    page.navigate_with_wait_post(url, wait_until, nav_method, nav_body).await.map_err(|e| e.to_string())?;
+                    page.navigate_with_wait_post_with_preloads(url, wait_until, nav_method, nav_body, &preload_scripts).await.map_err(|e| e.to_string())?;
                 } else {
-                    page.navigate_with_wait(url, wait_until).await.map_err(|e| e.to_string())?;
-                }
-
-                for source in &preload_scripts {
-                    if let Err(e) = page.execute_preload_script(source) {
-                        tracing::debug!("Preload script error: {}", e);
-                    }
+                    page.navigate_with_wait_with_preloads(url, wait_until, &preload_scripts).await.map_err(|e| e.to_string())?;
                 }
 
                 let reached_network_idle = page.lifecycle.is_network_idle();
--- a/crates/obscura-cdp/src/server.rs
+++ b/crates/obscura-cdp/src/server.rs
@@ -424,14 +424,9 @@
             Some(obscura_js::ops::InterceptResolution::Fulfill { status, headers, body }) => {
                 let _v8_guard = obscura_js::v8_lock::global().lock().await;
                 let result = page
-                    .fulfill_navigation_html(&url_owned, &body, status, &headers, wait_until)
+                    .fulfill_navigation_html_with_preloads(&url_owned, &body, status, &headers, wait_until, &preload_scripts)
                     .await
                     .map_err(|e| e.to_string());
-                for source in &preload_scripts {
-                    if let Err(e) = page.execute_preload_script(source) {
-                        tracing::debug!("Preload script error: {}", e);
-                    }
-                }
                 drop(_v8_guard);
                 (result, Some(page))
             }
@@ -446,12 +441,7 @@
                     // CDP messages via `dispatch` (which also acquires this lock); both
                     // sides must coordinate or V8 aborts the process at concurrency >= 5.
                     let _v8_guard = obscura_js::v8_lock::global().lock().await;
-                    let result = page.navigate_with_wait(&url_owned, wait_until).await.map_err(|e| e.to_string());
-                    for source in &preload_scripts {
-                        if let Err(e) = page.execute_preload_script(source) {
-                            tracing::debug!("Preload script error: {}", e);
-                        }
-                    }
+                    let result = page.navigate_with_wait_with_preloads(&url_owned, wait_until, &preload_scripts).await.map_err(|e| e.to_string());
                     drop(_v8_guard);
                     let _ = nav_done_tx.send((page, result)).await;
                 });
--- a/crates/obscura-js/src/runtime.rs
+++ b/crates/obscura-js/src/runtime.rs
@@ -94,6 +94,12 @@
         let mut state = self.state.borrow_mut();
         state.intercept_tx = Some(tx);
         state.intercept_enabled = true;
+    }
+
+    pub fn clear_intercept_tx(&self) {
+        let mut state = self.state.borrow_mut();
+        state.intercept_tx = None;
+        state.intercept_enabled = false;
     }
 
     pub fn set_user_agent(&mut self, ua: &str) {
--- a/crates/obscura-cdp/src/domains/fetch.rs
+++ b/crates/obscura-cdp/src/domains/fetch.rs
@@ -93,8 +93,7 @@
             ctx.fetch_intercept.enabled = false;
             ctx.fetch_intercept.patterns.clear();
             if let Some(page) = ctx.get_session_page_mut(session_id) {
-                page.intercept_enabled = false;
-                page.intercept_block_patterns.clear();
+                page.clear_intercept_tx();
             }
             let paused: Vec<_> = ctx.fetch_intercept.paused.drain().collect();
             for (_, req) in paused {
